# Areas that could be improved
- Adding an optional Hotel filter to the GET /rooms endpoint
- Using more targeted setup and clear down code for integration tests (instead of relying on /database/clear and /database/seed)
- Allow a booking to be made against a Hotel and Room Type, instead of directly against a given room